/*static void testPut(char *url, int fromParent, int toParent,
		GList *requestHeaders, GList *requestParameters, gboolean *responseQueued,
		err_t *e)
{
	mark();
	//TODO
}*/

/*static void testPost(char *url, int fromParent, int toParent,
		GList *requestHeaders, GList *requestParameters, gboolean *responseQueued,
		err_t *e)
{
	mark();
	//TODO
}*/

/*static void testDel(char *url, int fromParent, int toParent,
		GList *requestHeaders, GList *requestParameters, gboolean *responseQueued,
		err_t *e)
{
	mark();

	//TODO
}*/

/*static void testGet(char *url, int fromParent, int toParent,
		GList *requestHeaders, GList *requestParameters, gboolean *responseQueued,
		err_t *e)
{
	mark();

	char page[1000];
	snprintf(page, sizeof(page), "<html><body>Hello %s!<"
			"/body></html>",
			url);
	terror(respondFromBuffer(toParent, MHD_HTTP_OK, page,
			strlen(page) + 1, responseQueued, e))
finish:
	return;
}*/
